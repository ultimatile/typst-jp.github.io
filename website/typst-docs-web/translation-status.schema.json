{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "Page Translation Status",
	"description": "A JSON file for managing the translation status of each page",
	"type": "object",
	"properties": {
		"$schema": {
			"type": "string",
			"description": "Reference to the JSON schema used in this file"
		}
	},
	"additionalProperties": {
		"type": "string",
		"enum": ["translated", "partially_translated", "untranslated", "community"],
		"description": "The translation status of the page"
	}
}
